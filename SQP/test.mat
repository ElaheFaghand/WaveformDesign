clc
clear
N = 10;
M = 10;
R_o = importdata("R.mat");
P_R = Beam_Pattern (N, theta, R);
plot(theta*180/pi ,10*log10(abs(P_R))),hold on,  grid on
 while delta > 0   
    phi = rand(N,M);
    X = exp(1j*2*pi*phi);
    R = 1/N *(X'*X);
    delta = R - R_o;
 end
